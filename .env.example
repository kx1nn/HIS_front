# 后端 API 地址
VITE_API_BASE=http://localhost:8080
VITE_API_TARGET=http://localhost:8080

# Sentry 错误监控（可选）
# 启用后将自动上报生产环境错误到 Sentry
# 使用方法：
# 1. 在 https://sentry.io 创建项目并获取 DSN
# 2. 安装依赖：npm install @sentry/browser
# 3. 设置以下环境变量
VITE_SENTRY_ENABLED=false
VITE_SENTRY_DSN=
